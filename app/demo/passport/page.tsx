import Link from "next/link";
import { PassportBooklet } from "@/components/passport-booklet";
import { demoData } from "@/lib/mock-data";

export default function DemoPassportPage() { return <main className="min-h-screen bg-cream"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-6 sm:px-8"><Link href="/demo" className="text-sm font-semibold text-moss">← Back to Demo</Link><span className="text-xs font-bold tracking-[.15em] text-saffron">PROPERTY PASSPORT</span></header><section className="mx-auto grid max-w-6xl gap-10 px-5 pb-16 pt-8 sm:px-8 lg:grid-cols-[.8fr_1.2fr] lg:items-center"><div><p className="text-xs font-bold tracking-[.16em] text-saffron">CITIZEN CREDENTIAL</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.06em] sm:text-6xl">One identity. Every verified property.</h1><p className="mt-5 max-w-md text-lg leading-8 text-[#59685e]">A physical-looking credential that leads naturally to the citizen’s secure digital Property Passport.</p><dl className="mt-8 grid gap-3 sm:grid-cols-2"><Info label="Owner" value={demoData.owner}/><Info label="Property count" value="1 verified property"/><Info label="Issued" value={demoData.issueDate}/><Info label="Credential" value={demoData.credentialVersion}/></dl></div><div className="mx-auto w-full max-w-2xl"><PassportBooklet/></div></section></main>; }

function Info({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl border border-[#dbe4da] bg-white p-4"><dt className="text-[10px] font-bold tracking-[.13em] text-[#748177]">{label.toUpperCase()}</dt><dd className="mt-2 font-semibold">{value}</dd></div>; }
