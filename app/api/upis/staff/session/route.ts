import { NextResponse } from "next/server";
import { createStaffSession } from "@/lib/upis-staff-session";
export async function POST(request: Request) { const body = await request.json() as { accessCode?: string }; const code = body.accessCode; const staff = code === process.env.UPIS_DEMO_OFFICER_CODE ? { id: "KA-0472" as const, role: "OFFICER" as const } : code === process.env.UPIS_DEMO_SUPERVISOR_CODE ? { id: "SUP-019" as const, role: "SUPERVISOR" as const } : undefined; if (!staff) return NextResponse.json({ error: "Invalid demo staff access code." }, { status: 401 }); const response = NextResponse.json({ role: staff.role, staffId: staff.id }); response.cookies.set("upis_staff_session", createStaffSession(staff), { httpOnly: true, sameSite: "lax", secure: process.env.NODE_ENV === "production", path: "/", maxAge: 60 * 60 * 8 }); return response; }
