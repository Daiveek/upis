"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
export default function CreateAccount() { const router = useRouter(); const [error, setError] = useState(""); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = new FormData(event.currentTarget); const name = String(data.get("name") || "").trim(); const mobile = String(data.get("mobile") || "").trim(); const email = String(data.get("email") || "").trim(); const pan = String(data.get("pan") || "").trim().toUpperCase(); if (!name || !mobile || !email || !pan) return setError("Please complete your name, mobile number, email and PAN."); try { const response = await fetch("/api/upis/citizens", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, mobile, email, pan }) }); const result = await response.json() as { error?: string }; if (!response.ok) { setError(result.error ?? "We couldn’t create your UPIS identity."); return; } router.push("/register"); } catch { setError("We couldn’t reach the UPIS service. Please try again."); } }; return <main className="min-h-screen bg-cream"><header className="mx-auto max-w-xl px-5 py-7"><Link href="/" className="text-sm font-semibold text-moss">← UPIS</Link></header><section className="mx-auto max-w-xl px-5 pb-16"><p className="text-xs font-bold tracking-[.16em] text-saffron">CREATE YOUR UPIS IDENTITY</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.05em]">One identity for every property you own.</h1><p className="mt-4 leading-6 text-[#617066]">Your PAN helps ensure one person cannot accidentally create multiple Property Passports using different contact details.</p><p className="mt-3 rounded-xl bg-[#fff9eb] p-3 text-sm text-[#74521f]">Use fictional/demo details for this competition prototype. Do not enter sensitive real-world information.</p><form onSubmit={submit} className="mt-8 rounded-[1.75rem] bg-white p-6 shadow-card"><label className="block text-sm font-semibold">Full name<input name="name" autoComplete="name" className="mt-2 w-full rounded-xl border border-[#c8d3c7] px-4 py-3 outline-none focus:border-moss" /></label><label className="mt-5 block text-sm font-semibold">Mobile number<input name="mobile" inputMode="tel" autoComplete="tel" className="mt-2 w-full rounded-xl border border-[#c8d3c7] px-4 py-3 outline-none focus:border-moss" /></label><label className="mt-5 block text-sm font-semibold">Email<input name="email" type="email" autoComplete="email" className="mt-2 w-full rounded-xl border border-[#c8d3c7] px-4 py-3 outline-none focus:border-moss" /></label><label className="mt-5 block text-sm font-semibold">PAN<input name="pan" placeholder="ABCDE1234F" maxLength={10} className="mt-2 w-full rounded-xl border border-[#c8d3c7] px-4 py-3 uppercase outline-none focus:border-moss" /></label>{error && <p className="mt-4 rounded-xl bg-[#fff5e9] p-3 text-sm text-[#7b542c]">{error}</p>}<button className="mt-7 w-full rounded-2xl bg-moss px-5 py-4 font-semibold text-white">Create my Property Passport</button></form></section></main>; }
